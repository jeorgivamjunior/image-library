import { render } from '@testing-library/react';

describe('ImageUploader file', () => {
  // should render the icon with there is no image
  // should render the image
  // should render start with a image and render the icon after clicking on the remove button
});
