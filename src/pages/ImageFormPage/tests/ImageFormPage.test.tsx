import { render } from '@testing-library/react';

describe('ImageFormPage file', () => {
  // should not render the dialog when isOpen is false
  // should render the dialog when isOpen is true
  // should render the form and its text field
  // should render the diabled button when title is empty and should render the actived button when title is not empty
});
