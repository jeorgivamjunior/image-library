import { render } from '@testing-library/react';

describe('GridList file', () => {
  // should render a table with 2 items
  // should render the sixth item on the screen after click on the next page
});
